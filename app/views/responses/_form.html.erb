<%= form_tag('/responses') do |f| %>

  <% @questions.each do |question| %>
    <%= JsonFormBuilder.new(question).render %>
  <% end %>

  <div class="actions">
    <%= submit_tag %>
  </div>
<% end %>
